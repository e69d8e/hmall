spring:
  application:
    name: cart-service # 服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.6.100 # nacos地址
      config:
        server-addr: 192.168.6.100
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared.yaml # 共享mybatis配置

