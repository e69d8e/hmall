spring:
  application:
    name: cart-service # 服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 # nacos地址
      config:
        server-addr: 127.0.0.1
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared.yaml # 共享配置
          - data-id: shared-seata.yaml # 共享seata配置